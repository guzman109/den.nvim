# Personal website

Status: active
Stack: typescript, tailwind, vite

## Outcome

A small site with three projects on it, written in my own voice, that I can update in under ten minutes.

## Next actions

- [ ] Gather a few visual references @tag(design) @tag(research) @id(web-references) @status(backlog) @order(2000000)
- [ ] Draft the homepage story @tag(writing) @tag(deep-work) @due(2026-09-20) @id(web-story) @status(doing) @order(1000000)
- [ ] Choose three projects to feature @tag(writing) @id(web-projects) @status(backlog) @order(3000000)
- [x] Register the domain @tag(admin) @tag(web) @id(web-domain) @status(done) @order(1000000)

## Notes

Keep the writing plain. No carousel, no hero video.

Domain renews every March.
