# A quieter workspace

Status: active

## Outcome

Less friction between having an idea and getting started.

## Next actions

- [x] Clear the desk @tag(studio) @id(studio-clear) @status(done) @order(2000000)
- [ ] Sketch the desk layout @tag(studio) @tag(design) @id(studio-layout) @status(backlog) @order(5000000)
- [ ] Sort the reference shelf @tag(studio) @tag(admin) @id(studio-shelf) @status(backlog) @order(6000000)
- [ ] Make a playlist for deep work @tag(deep-work) @id(studio-music) @status(doing) @order(4000000)

## Notes

The shelf is the part that actually slows things down.
